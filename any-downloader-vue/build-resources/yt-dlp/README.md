# yt-dlp（Windows 可选内置）

构建 Windows 安装包前由 `scripts/fetch-ytdlp-win.cjs` 下载官方 `yt-dlp.exe` 到此目录，并由 `electron-builder` 的 `extraResources` 复制到安装目录 `resources/yt-dlp/`。

许可证：**Unlicense**（以 [yt-dlp/yt-dlp](https://github.com/yt-dlp/yt-dlp) 仓库为准）。
